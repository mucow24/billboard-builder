import { EditorPage } from './helpers/editorPage';
import { expect, test } from './helpers/test';
test.describe('@p1 tool modes and cursor affordances',()=>{
test('shift temporarily switches the cursor to a hand and restores it after keyup', async ({page})=>{const editor=new EditorPage(page);await editor.goto();await expect(editor.stage).toHaveCSS('cursor','default');await page.keyboard.down('Shift');await expect(editor.stage).toHaveCSS('cursor','grab');await page.keyboard.up('Shift');await expect(editor.stage).toHaveCSS('cursor','default');await page.getByRole('button',{name:/Rect/i}).click();await expect(editor.stage).toHaveCSS('cursor','crosshair');await page.keyboard.down('Shift');await expect(editor.stage).toHaveCSS('cursor','grab');await page.keyboard.up('Shift');await expect(editor.stage).toHaveCSS('cursor','crosshair');});
test('hand and zoom tools expose the expected cursors and interaction modes', async ({page})=>{const editor=new EditorPage(page);await editor.goto();await page.getByRole('button',{name:/Hand/i}).click();await expect(editor.stage).toHaveCSS('cursor','grab');const stageBox=await editor.stageBox();await page.mouse.move(stageBox.x+420,stageBox.y+280);await page.mouse.down();await expect(editor.stage).toHaveCSS('cursor','grabbing');await page.mouse.move(stageBox.x+520,stageBox.y+340,{steps:12});await page.mouse.up();await page.getByRole('button',{name:/Zoom/i}).click();await expect(editor.stage).toHaveCSS('cursor','zoom-in');const before=(await editor.viewport()).zoom;await page.mouse.click(stageBox.x+420,stageBox.y+280);await expect.poll(async()=> (await editor.viewport()).zoom).toBeGreaterThan(before);});
test('escape exits create mode and hotkeys still switch tools after overlay interaction', async ({page})=>{const editor=new EditorPage(page);await editor.goto();await page.getByRole('button',{name:/Rect/i}).click();await page.keyboard.press('Escape');await expect(page.getByRole('button',{name:/Arrow/i})).toHaveAttribute('aria-pressed','true');await page.getByLabel('Canvas width').click();await page.keyboard.press('KeyH');await expect(page.getByRole('button',{name:/Arrow/i})).toHaveAttribute('aria-pressed','true');await page.locator('.properties-panel').click();await page.keyboard.press('KeyH');await expect(page.getByRole('button',{name:/Hand/i})).toHaveAttribute('aria-pressed','true');await page.keyboard.press('KeyZ');await expect(page.getByRole('button',{name:/Zoom/i})).toHaveAttribute('aria-pressed','true');await page.keyboard.press('KeyV');await expect(page.getByRole('button',{name:/Arrow/i})).toHaveAttribute('aria-pressed','true');});});