import { EditorPage } from './helpers/editorPage';
import { expect, test } from './helpers/test';
test.describe('@p0 layers and ordering coverage',()=>{
test('layers panel toggles, background color changes, and layer selection drives the panel', async ({page})=>{const editor=new EditorPage(page);await editor.goto();await editor.createItem('Rect');await editor.createItem('Text');await page.getByRole('button',{name:/Layers/i}).click();await expect(page.getByRole('button',{name:'Canvas background'})).toHaveCount(0);await page.getByRole('button',{name:/Layers/i}).click();await expect(page.getByRole('button',{name:'Canvas background'})).toBeVisible();await editor.setColorHex('Canvas background','#123456ff');await editor.selectLayer('Rectangle');await expect(page.getByRole('heading',{name:'Rectangle'})).toBeVisible();await editor.selectLayer('Text');await expect(page.getByRole('heading',{name:'Text'})).toBeVisible();});
test('toolbar ordering buttons and layer deletion update the right items', async ({page})=>{const editor=new EditorPage(page);await editor.goto();await editor.createItem('Rect');await editor.createItem('Ellipse');await editor.createItem('Text');await editor.selectLayer('Rectangle',2);await page.getByRole('button',{name:'Bring front'}).click();await expect(editor.layerRows.first()).toContainText('Rectangle');await page.getByRole('button',{name:'Backward'}).click();await expect(editor.layerRows.nth(1)).toContainText('Rectangle');await page.getByRole('button',{name:'Send back'}).click();await expect(editor.layerRows.last()).toContainText('Rectangle');await page.getByRole('button',{name:'Delete Rectangle'}).click();await expect(editor.layerRows).toHaveCount(2);});
test('background color and layer ordering survive save and reopen', async ({page})=>{const editor=new EditorPage(page);await editor.goto();await editor.createItem('Rect');await editor.createItem('Text');await editor.selectLayer('Rectangle',1);await page.getByRole('button',{name:'Bring front'}).click();await editor.setColorHex('Canvas background','#0f172aff');const download=page.waitForEvent('download');await page.getByRole('button',{name:'Save'}).click();const dl=await download;const p=await dl.path();if(!p) throw new Error('No project path');await page.getByRole('button',{name:'New'}).click();await editor.openProject(p);await expect(editor.layerRows).toHaveCount(2);await expect(editor.layerRows.first()).toContainText('Rectangle');await expect(page.getByRole('button',{name:'Canvas background'})).toContainText('#0f172aff');});});